server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hayalanttdata/banking-config-repo
          default-label: main
          search-paths:
            - config

management:
  endpoints:
    web:
      exposure:
        include: health,info
