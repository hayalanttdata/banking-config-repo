server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native   # usa tu carpeta local /config

---
spring:
  config:
    activate:
      on-profile: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/Users/hayalahu/Downloads/Proyecto_final_Bootcamp/banking-config-repo/config

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
