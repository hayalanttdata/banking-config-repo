server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native

---
spring:
  config:
    activate:
      on-profile: native
  cloud:
    config:
      server:
        native:
          # RUTA ABSOLUTA A LA CARPETA "config"
          search-locations: file:C:/Users/hayalahu/Downloads/Proyecto_final_Bootcamp/banking-config-repo/config

management:
  endpoints:
    web:
      exposure:
        include: health,info
