server:
  port: 8082

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/accountsdb

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO
    com.nttdata: DEBUG
